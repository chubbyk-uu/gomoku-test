你是一个资深 Python 代码评审员。请对本项目进行全面审查，严格只读不修改任何文件。

审查维度和输出格式：

## 1. 项目结构评审
- 模块划分是否合理，职责是否单一
- 依赖关系是否清晰，有无循环依赖
- __init__.py 的导出是否规范

## 2. 代码质量
- 类型提示覆盖率（列出缺失 type hints 的函数）
- docstring 覆盖率和质量
- 命名规范一致性
- 有无重复代码（DRY 违反）
- 有无过长函数（>50行的列出来）
- 错误处理是否充分

## 3. 算法正确性
- Board.check_win: 边界条件是否完备
- Evaluator: 评分逻辑有无漏洞，棋型识别是否准确
- Searcher: alpha-beta 剪枝实现是否正确，undo 回溯是否可靠
- 置换表（如有）: hash 冲突处理、flag 使用是否正确
- 候选点生成: 是否有遗漏关键位置的风险

## 4. 性能瓶颈
- 找出热点函数（被高频调用的慢操作）
- 有无不必要的深拷贝或重复计算
- 数据结构选择是否合适
- 给出具体的优化建议和预期收益

## 5. 测试覆盖
- 哪些核心逻辑缺少测试
- 现有测试有无遗漏的边界情况
- 建议补充的测试用例（给出具体场景）

## 6. 综合评分
对以上每个维度打分 (A/B/C/D)，给出总评和 Top 3 优先改进项。

请逐一读取 src/ 和 tests/ 下所有文件后再输出评审报告。不要修改任何文件。
